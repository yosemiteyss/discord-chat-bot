version: "3"
services:
  discord-chat-bot:
    build:
      context: ../
      dockerfile: Dockerfile
    container_name: discord-chat-bot
    restart: unless-stopped
    environment:
      - CHAT_SERVICE=azure